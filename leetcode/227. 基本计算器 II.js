/**
 * @param {string} s
 * @return {number}
 */
var calculate = function(s) {
    let _t = ['*', '/', '+', '-']
    const symbols = [], nums = []
    for (let i = 0, len = s.length; i < len, i++;) {
        const item = s[i]
        if (_t.includes(item)) {

        }
    }
};